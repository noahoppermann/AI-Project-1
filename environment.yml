# Course environment for the search assignment.
#
#   conda env create -f environment.yml
#   conda activate ai-search
#
# After this file changes:
#   conda env update -f environment.yml --prune
name: ai-search
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy=2.*
  - pytest=8.*
